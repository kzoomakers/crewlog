# Flask Configuration
FLASK_ENV=development

# Security - CHANGE THIS to a random secret key in production!
SECRET_KEY=change-me-to-something-secure

# Database Configuration
# For SQLite (default, data persists in ./crewlog/resources/database.db)
# Note: Use absolute path with 4 slashes for Docker: sqlite:////app/crewlog/resources/database.db
SQLALCHEMY_DATABASE_URI=sqlite:////app/crewlog/resources/database.db

# For PostgreSQL (uncomment and configure if needed)
# SQLALCHEMY_DATABASE_URI=postgresql://username:password@localhost:5432/togger_db

# Application URL (used in emails)
APP_URL=http://localhost:5001

# SMTP Configuration (for email verification and password recovery)
# Leave empty to disable email features
SMTP_LOGIN=
SMTP_MAILBOX=
SMTP_PASSWORD=
SMTP_PORT=
SMTP_SERVER=

# Example SMTP configuration for Gmail:
# SMTP_LOGIN=your-email@gmail.com
# SMTP_MAILBOX=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_PORT=587
# SMTP_SERVER=smtp.gmail.com