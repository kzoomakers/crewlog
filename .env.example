# Flask Configuration
FLASK_ENV=development

# Security - CHANGE THIS to a random secret key in production!
SECRET_KEY=change-me-to-something-secure

# Database Configuration
# For SQLite (default, data persists in ./togger/resources/database.db)
SQLALCHEMY_DATABASE_URI=sqlite:///resources/database.db

# For PostgreSQL (uncomment and configure if needed)
# SQLALCHEMY_DATABASE_URI=postgresql://username:password@localhost:5432/togger_db

# Application URL (used in emails)
APP_URL=http://localhost:5001

# Email Configuration (for email verification and password recovery)
# You can configure email via the Admin panel or use environment variables below.
# The Admin panel configuration takes precedence over environment variables.

# ============ Standard SMTP Configuration ============
# Leave empty to disable email features (or configure via Admin panel)
SMTP_LOGIN=
SMTP_MAILBOX=
SMTP_PASSWORD=
SMTP_PORT=
SMTP_SERVER=

# Example SMTP configuration for Gmail:
# SMTP_LOGIN=your-email@gmail.com
# SMTP_MAILBOX=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_PORT=587
# SMTP_SERVER=smtp.gmail.com

# ============ SendGrid API Configuration ============
# Get your API key from https://sendgrid.com
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com
# SENDGRID_FROM_NAME=CrewLog

# ============ SMTP2GO API Configuration ============
# Get your API key from https://www.smtp2go.com
# SMTP2GO_API_KEY=api-xxxxxxxxxxxxx
# SMTP2GO_SENDER=noreply@yourdomain.com